; generated by drom from package skeleton 'library'

(library
  (name ez_text_renderer)
  (public_name ez_text_renderer)
  (wrapped false)
  (libraries )
  
  (foreign_stubs
    (language c)
    (names stubs text_renderer)
    (flags -I/usr/include/freetype2))
  (c_library_flags (-lfreetype))
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package ez_text_renderer))


